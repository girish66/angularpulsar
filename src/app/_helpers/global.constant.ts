import { Injectable } from '@angular/core';
import { environment } from '../../environments/environment';

@Injectable()
export class GlobalConstant {
  public static siteTitle: string = "Angular Pulsar Site";
}
